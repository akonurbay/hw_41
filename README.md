# hw_41

- Unit tests mock external services via mocktail (test/unit).
- Integration test starts a local fake server (test/integration) and runs scenario: login → list → add → logout.
- CI workflow runs flutter
# hw_41
